server:
  port: 8102
spring:
  application:
    name: eureka-client01
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8100/eureka/
    register-with-eureka: true
    fetch-registry: true
